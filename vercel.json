{
"builds": [
    {
      "src": "/api/main.py",
      "use": "@vercel/python"
    }]
  ,
  "routes": [
       { "src": "/:user/:project/archive/refs/:file.zip", "dest": "https://codeload.github.com/:user/:project/zip/refs/heads/:file" }，
    { "src": "/(.*)", "dest": "/api/main.py" }
       
  ]
}
